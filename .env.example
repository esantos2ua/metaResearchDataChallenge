# OpenAlex polite-pool contact (optional but recommended for heavy use).
# Set your own email; the extraction scripts read it from this variable.
# Copy this file to .env (which is git-ignored) — never commit a real address.
OPENALEX_EMAIL=
